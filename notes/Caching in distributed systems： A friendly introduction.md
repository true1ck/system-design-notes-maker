 You can also add images or videos in a separate section (either directly below this text block) using Markdown syntax: <img src="image url" /><br/>. If you want to use an image from the web that is not hosted on your website, please include its URL. For example, if I wanted to add a picture of my friend in front of me here's how it would look like: <img src="https://i.stack.imgur.com/wL5vU.jpg" />.
 You can also use Markdown syntax for code blocks using the following formatting options (either directly below this text block):: ````{language} Code goes here, including any markup or special characters that might be needed to render it correctly in your web browser. For example: <pre><code class="javascript">console.log('Hello World!')</code></pre>````
 You can also add code blocks using the following formatting options (either directly below this text block):: ```{language} Code goes here, including any markup or special characters that might be needed to render it correctly in your web browser. For example: <pre><code class="javascript">console.log('Hello World!')</code></pre>```
 You can also add code blocks using the following formatting options (either directly below this text block):: ```{language} Code goes here, including any markup or special characters that might be needed to render it correctly in your web browser. For example: <pre><code class="javascript">console.log('Hello World!')</code></pre>```
 You can also add code blocks using the following formatting options (either directly below this text block):: ```{language} Code goes here, including any markup or special characters that might be needed to render it correctly in your web browser. For example: <pre><code class="javascript">console.log('Hello World!')</code></pre>```
 You can also add code blocks using the following formatting options (either directly below this text block):: ```{language} Code goes here, including any markup or special characters that might be needed to render it